computational graph 작성=> 연산: 노드 변수: 엣지
input은 w_n과 x_n
(1)노드 미분 (2)변수에 순방향으로 계산한 결과값 대입, local gradient를 역방향에서 내려온 미분값과 곱한다
분기점: 같은 방식으로 하되 위의 (1)에서 변수가 2개일떄의 미분을 생각해야 함
분기에서 곱하기의 경우 기울기는 다른 것의 입력값, 더하기의 경우 1
x * y = z => 역전파 ∂x= y*∂z,   ∂y=x*∂z
x + y = z => 역전파 ∂x= 1*∂z,   ∂y=1*∂z
sigmoid의 경우, 미분 결과에 sigmoid 결과값이 나와있기 때문에, 결과값을 이용
